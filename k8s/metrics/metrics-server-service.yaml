apiVersion: v1
kind: Service

metadata:
  name: metrics-server
  namespace: data-pipelines

spec:
  selector:
    app: metrics-server

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP